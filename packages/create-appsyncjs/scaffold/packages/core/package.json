{
	"name": "@__APP_NAME__/core",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "Shared application code (GraphQL schema, AppSync JS resolvers)",
	"scripts": {
		"build:resolvers": "appsyncjs build -o ../../build/appsync -i 'src/resolvers/**/*.ts'",
		"test": "sst shell -- vitest run --coverage"
	},
	"dependencies": {
		"@mikecbrant/appsyncjs-dynamo": "^1.0.0"
	},
	"devDependencies": {
		"@aws-appsync/utils": "^2.0.3",
		"@mikecbrant/appsyncjs-test-utils": "^1.2.4",
		"@tsconfig/node22": "^22.0.0",
		"typescript": "^5.9.2",
		"vitest": "^3.2.4"
	},
	"engines": {
		"node": ">=22"
	}
}
