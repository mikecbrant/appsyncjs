{
	"name": "__APP_NAME__",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "__DESCRIPTION__",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"build:resolvers": "pnpm -C packages/core build:resolvers",
		"clean": "rm -rf build coverage",
		"deploy": "sst deploy",
		"remove": "sst remove",
		"dev": "sst dev",
		"test": "pnpm -C packages/core test"
	},
	"devDependencies": {
		"@mikecbrant/appsyncjs-cli": "^1.0.2",
		"@tsconfig/node22": "^22.0.0",
		"sst": "^3.1.0",
		"typescript": "^5.9.2"
	},
	"engines": {
		"node": ">=22"
	}
}
