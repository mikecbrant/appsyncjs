{
	"name": "__APP_NAME__",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "__DESCRIPTION__",
	"scripts": {
		"build:resolvers": "appsyncjs build -o appsync -i 'src/resolvers/**/*.ts'",
		"deploy": "sst deploy",
		"remove": "sst remove",
		"dev": "sst dev",
		"test": "sst load-config -- vitest run --coverage"
	},
	"dependencies": {
		"@mikecbrant/appsyncjs-dynamo": "^1.0.0"
	},
	"devDependencies": {
		"@aws-appsync/utils": "2.0.3",
		"@mikecbrant/appsyncjs-cli": "^1.0.2",
		"@mikecbrant/appsyncjs-test-utils": "^1.2.4",
		"sst": "^3.1.0",
		"typescript": "^5.9.2",
		"vitest": "^3.2.4"
	},
	"engines": {
		"node": ">=22"
	}
}
